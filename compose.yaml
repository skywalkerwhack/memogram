services:
  memogram:
    image: your-dockerhub-username/memogram:latest
    container_name: memogram
    env_file: .env
    restart: unless-stopped
    environment:
      DATA: /app/data/data.txt
    volumes:
      - memogram-data:/app/data

volumes:
  memogram-data:
